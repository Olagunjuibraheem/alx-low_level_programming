#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Return: Always (0)
 */

int main(void)
{

	char alph = 'a';

	for (; alph <= 'z'; alph++)
	{
	putchar(alph);
	}
	putchar('\n');
	return (0);
}
